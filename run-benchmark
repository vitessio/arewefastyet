#!/bin/bash

#cd to directory (modify this according to your system folder destination)
cd /root/arewefastyet/

PYTHON_PATH=/root/arewefastyet/benchmark/bin/python
# virtualenv is now active.
#

#NEW_UUID=$(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1)

python run-benchmark.py $1 $(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1) $3